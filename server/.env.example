# MySQL connection (defaults shown)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=SkiResorts

# OpenWeather key for the weather panel and radar map.
# Keep it here on the server only; never put it in client/.env.
OPENWEATHER_API_KEY=your_openweather_api_key
